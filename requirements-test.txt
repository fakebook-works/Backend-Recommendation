# Pinned to exact versions so a CI run is reproducible and cannot start failing because
# an unrelated release changed behaviour. Versions match requirements.txt wherever the
# two overlap; keep them in step when either is updated.

fastapi==0.140.0
strawberry-graphql[fastapi]==0.323.2
greenlet==3.1.1
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
numpy==2.4.6
requests==2.34.2
python-dotenv==1.2.2
pytest==9.1.1
httpx==0.28.1
